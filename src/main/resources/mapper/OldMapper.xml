<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboot.mapper.OldMapper">
    <select id="selectCmsById" resultType="map">
        SELECT id, code, path  FROM `old` WHERE id = #{id}
    </select>
</mapper>

